import React from "react";
import { Loader } from "lucide-react";

const Loading = () => (
  <div className="flex min-h-screen w-full flex-1 items-center justify-center bg-primary">
    <Loader />
  </div>
);

export default Loading;
